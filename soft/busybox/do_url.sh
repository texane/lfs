# set LFS_RETURN_VALUE to the download location
# the location is an url, indicating the access
# scheme in file, http, https, ftp, git, svn, hg.
# execute in the main script context
LFS_RETURN_VALUE=http://www.busybox.net/downloads/busybox-$LFS_BUSYBOX_VERS.tar.bz2
